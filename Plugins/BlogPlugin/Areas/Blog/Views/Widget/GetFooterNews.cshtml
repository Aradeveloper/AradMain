@model IEnumerable<BlogPlugin.ViewModel.DataTablePost>

@if (Model != null)
{
    <div class="col-sm-3">
        <div class="widget">
            <strong class="widget-title upper">مطالب محبوب</strong>
            <ul class="post-list">
                @foreach (var item in Model)
                {
                    <li>
                        <strong><a href="@Url.Action(BMVC.Blog.Widget.ActionNames.Detailes, BMVC.Blog.Widget.Name, new { Id = item.Id, area = BMVC.Blog.Name })">@Html.DisplayFor(a => item.Title)</a></strong>
                        <em>@Html.ConvertToPersianDateTime(item.Timestamp)</em>
                    </li>
                }
            </ul>
            <!-- /.post-list -->
        </div>
        <!-- /.widget -->
    </div>

}